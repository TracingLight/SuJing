# 用户原始素材

把希望用于网站的原始图片放在这个目录中。可以按主题继续建立子目录，例如：

```text
assets/user/
  avatar/
  game-projects/
  illustrations/
  screenshots/
  life/
```

这里保存原图，不直接由 Hexo 发布。我会根据页面用途选择合适的图片，进行裁切、压缩、重命名和格式转换，再把站点实际使用版本放入 `source/img/user/`。

建议使用有含义的文件名，例如 `unity-combat-demo.png`，不要使用大量无法区分的 `IMG_001.png`。
